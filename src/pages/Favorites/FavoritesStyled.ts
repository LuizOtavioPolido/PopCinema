import styled from 'styled-components/native';
import { colors } from '../../utils/Theme';

export const FavortiesStyled = {
  Content: styled.View`
    background-color: ${colors.background};
    flex: 1;
    flex-direction: column;
  `,
};
